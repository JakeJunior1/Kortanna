# Primer — ~/Developer (current state)

> The cross-project **state snapshot** a planner reads at session start. Keep it short (done / in-progress /
> next + pointers) — never a growing changelog. Detail lives in each project's own `memory/`. Dates absolute
> (YYYY-MM-DD). This is a starter skeleton — fill it in as you go.

## Environment
- _(your machine + layout: `projects/` personal · `clients/` client work · `guide-setup/` the method)_

## In flight
- _(what's being worked on across projects, each with a pointer to its own `memory/`/`planning/`)_

## Next
- _(what's queued cross-project)_
