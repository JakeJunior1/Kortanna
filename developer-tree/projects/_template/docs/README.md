# docs/

Project-facing documentation — design notes, runbooks, API/usage docs, decisions that aren't ADRs.
Fill at stamp time or delete if the project doesn't need it. (ADRs live in `memory/decisions.md`;
planning specs live in `planning/`.)
