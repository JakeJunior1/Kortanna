# Lessons — {{PROJECT_NAME}} (project-specific)

> What THIS project taught us — distilled from native auto-memory `feedback-*` captures via `/wrap` + `/groom`.
> Project-scoped: how planners orchestrate across **every** project lives in `~/Developer/memory/lessons.md`,
> universal truths graduate to `~/.claude/rules/`. Actionable directives, not a changelog. Dates absolute.

- <lesson> — **Why:** <…> **How to apply:** <…>
